# frontend-app

Minimal Next.js + TypeScript + Tailwind scaffold for SportsApp.

Install and run:

```bash
cd frontend-app
npm install
npm run dev
```

To initialize Tailwind (already configured here):

```bash
npx tailwindcss init -p
```
